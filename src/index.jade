extends ../layouts/default

block content
  .container
    // Header
    include header.jade
    
    // Body
    - for (var i = 0; i < list.length; i+=3) {
      .row.demo-row
        - for (var j = 0; j < 3 && i + j < list.length; j++) {  
          .col-xs-4
            a(href="#{repodir}/sources/#{list[i + j]}")
              img(src="#{reporaw}/sources/#{list[i + j]}/result.jpg" width="250")
        - }
    - }
    
